<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: "Times New Roman", serif; font-size: 12pt; color: #000; margin: 0; padding: 0; }
        .wrap { padding-top: 120px; text-align: center; }
        .label { font-size: 14pt; font-weight: 700; text-transform: uppercase; letter-spacing: 1px; margin-bottom: 12px; }
        .name { font-size: 12pt; }
    </style>
</head>
<body>
    <div class="wrap">
        <div class="label">Lampiran {{ $number }}</div>
        <div class="name">{{ $name }}</div>
    </div>
</body>
</html>
